version: '3.9'

services:

  django:
    image: django_local
    build:
      context: ./django
    env_file:
      - .env.dev
    ports:
      - 8000:8000
    volumes:
      - ./django/:/app
    environment:
      - VSCODE_DEV=1