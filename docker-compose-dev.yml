version: '3.9'

services:

  django:
    image: django_dev
    build:
      context: ./djangosite
    env_file:
      - .env.dev
    ports:
      - 8000:8000
    volumes:
      - ./djangosite/:/app
    command: python manage.py runserver 0.0.0.0:8000
